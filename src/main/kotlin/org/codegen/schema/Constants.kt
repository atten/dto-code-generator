package org.codegen.schema

class Constants {
    companion object {
        // sentinel value (internal placeholder for optional unset fields)
        const val UNSET = "<UNSET>"
        // valid value for fields deliberately set empty in schema
        const val EMPTY = "EMPTY"
    }
}
